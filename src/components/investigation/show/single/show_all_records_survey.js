import React from 'react'
import PropTypes from 'prop-types';

export default function ShowAllRecordsSurvey() {
    return (
        <div>
            ShowAllRecordsSurvey
        </div>
    )
}

ShowAllRecordsSurvey.propTypes = {
    records: PropTypes.array,
    survey: PropTypes.object
};
